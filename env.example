# =============================================================================
# OpenTelemetry Observability Stack - Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed:
#   cp env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Service Ports
# -----------------------------------------------------------------------------
JAEGER_UI_PORT=16686
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
LOKI_PORT=3100
OTEL_GRPC_PORT=4317
OTEL_HTTP_PORT=4318
SEQ_UI_PORT=5380
SEQ_API_PORT=5341

# -----------------------------------------------------------------------------
# Grafana Configuration
# -----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
GRAFANA_ROOT_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Data Retention (default: 30 days for all)
# -----------------------------------------------------------------------------
# Traces retention (Jaeger with Badger storage)
# Format: Go duration (e.g., 720h = 30 days, 168h = 7 days, 2160h = 90 days)
TRACES_RETENTION=720h

# Metrics retention (Prometheus)
# Format: Prometheus duration (e.g., 30d, 15d, 90d)
METRICS_RETENTION=30d

# Logs retention (Loki)
# Format: Go duration (e.g., 720h = 30 days, 168h = 7 days, 2160h = 90 days)
LOGS_RETENTION=720h

# -----------------------------------------------------------------------------
# Service Metadata (added to all telemetry)
# -----------------------------------------------------------------------------
SERVICE_NAMESPACE=default
DEPLOYMENT_ENV=development

# -----------------------------------------------------------------------------
# Seq Configuration (optional - for .NET apps)
# -----------------------------------------------------------------------------
# To generate a password hash, run:
#   echo 'your-password' | docker run --rm -i datalust/seq config hash
# SEQ_ADMIN_PASSWORD_HASH=
