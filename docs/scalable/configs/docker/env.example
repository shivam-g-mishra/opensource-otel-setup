# Environment Configuration for Scalable Observability Stack
#
# Copy this file to .env and customize for your environment
# Usage: docker compose --env-file .env -f docker-compose-scalable.yaml up -d

# =============================================================================
# Grafana Configuration
# =============================================================================
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
# Change this to your actual URL in production
GF_SERVER_ROOT_URL=http://localhost:3000

# =============================================================================
# Kafka Configuration
# =============================================================================
KAFKA_RETENTION_HOURS=24
KAFKA_NUM_PARTITIONS=12

# =============================================================================
# Resource Limits (adjust based on your hardware)
# =============================================================================
# OTel Gateway memory limit (MiB)
OTEL_GATEWAY_MEMORY_LIMIT=1600
# OTel Processor memory limit (MiB)
OTEL_PROCESSOR_MEMORY_LIMIT=3200

# =============================================================================
# Retention Settings
# =============================================================================
# Prometheus retention (for stack self-monitoring)
PROMETHEUS_RETENTION=7d
# Tempo trace retention
TEMPO_RETENTION_HOURS=720
# Loki log retention
LOKI_RETENTION_HOURS=168
# Mimir metrics retention
MIMIR_RETENTION_HOURS=744

# =============================================================================
# Scaling Configuration
# =============================================================================
# Number of gateway replicas
OTEL_GATEWAY_REPLICAS=2
# Number of processor replicas
OTEL_PROCESSOR_REPLICAS=2
